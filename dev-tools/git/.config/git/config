[user]
    name = Joshua Li
    email = josh@jrl.ninja
    signingkey = 846B322709634596!

[url "git@github.com:"]
    insteadOf = https://github.com/

[url "git@gitlab.com:"]
    insteadOf = https://gitlab.com/

[core]
    autocrlf = input
    compression = 9

[alias]
    amend = commit --amend -v
    b = blame -c --date=short
    c = checkout
    d = diff
    dc = diff --cached
    s = show
    l = log --date=short --pretty='format:%h %ad (%an)%d %s'
    unstage = reset --

[pager]
    log = less
    diff = ydiff | less
    show = ydiff | less

[color]
    status = always
    diff = never  # disable colors for ydiff and patch generation

[push]
    default = current
    followTags = false

[branch]
    autoSetupMerge = always

[pull]
    rebase = true

[rebase]
    autoStash = true
